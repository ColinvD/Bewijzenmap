using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Opdracht : MonoBehaviour {

    /*
         maak een object dat als je rijd("spatie") je liters afloopt en als je tank leeg is vul je weer bij (door op "r" te drukken).
         als je rijd zegt je object "vroem" en als hij leeg is zegt hij "Tank is leeg"
         pas je kennis toe van Encapsulation, inheritance, abstractions
    */

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
